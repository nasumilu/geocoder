# PHP Geocoder

The intention for this component is to provide an interface which can be used
by different Geocoding services.

## Install via Composer
```bash
$ composer install nasumilu/geocoder
```
